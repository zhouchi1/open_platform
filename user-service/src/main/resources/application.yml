server:
  port: 8555
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: '*'

dubbo:
  application:
    name: user-service
    qos-enable: false
  registry:
    address: nacos-headless.default.svc.cluster.local:8848
    register-mode: instance
  protocol:
    name: dubbo
    port: 28555