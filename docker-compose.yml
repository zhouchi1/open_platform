version: '3.8'

services:
  ai-service:
    build:
      context: ./ai-service
      dockerfile: Dockerfile
    container_name: ai-service
    environment:
      - SW_AGENT_NAME=ai-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    container_name: auth-service
    environment:
      - SW_AGENT_NAME=auth-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  email-service:
    build:
      context: ./email-service
      dockerfile: Dockerfile
    container_name: email-service
    environment:
      - SW_AGENT_NAME=email-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  gateway-service:
    build:
      context: ./gateway-service
      dockerfile: Dockerfile
    container_name: gateway-service
    environment:
      - SW_AGENT_NAME=gateway-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  message-service:
    build:
      context: ./message-service
      dockerfile: Dockerfile
    container_name: message-service
    environment:
      - SW_AGENT_NAME=message-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  order-service:
    build:
      context: ./order-service
      dockerfile: Dockerfile
    container_name: order-service
    environment:
      - SW_AGENT_NAME=order-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  pay-service:
    build:
      context: ./pay-service
      dockerfile: Dockerfile
    container_name: pay-service
    environment:
      - SW_AGENT_NAME=pay-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  promotion-service:
    build:
      context: ./promotion-service
      dockerfile: Dockerfile
    container_name: promotion-service
    environment:
      - SW_AGENT_NAME=promotion-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  task-service:
    build:
      context: ./task-service
      dockerfile: Dockerfile
    container_name: task-service
    environment:
      - SW_AGENT_NAME=task-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    container_name: user-service
    environment:
      - SW_AGENT_NAME=user-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  websocket-service:
    build:
      context: ./websocket-service
      dockerfile: Dockerfile
    container_name: websocket-service
    environment:
      - SW_AGENT_NAME=websocket-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  xxl-job-admin:
    build:
      context: ./xxl-job-admin
      dockerfile: Dockerfile
    container_name: xxl-job-admin
    environment:
      - SW_AGENT_NAME=xxl-job-admin
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network

  sentinel-dashboard-service:
    build:
      context: ./sentinel-dashboard-service
      dockerfile: Dockerfile
    container_name: sentinel-dashboard-service
    environment:
      - SW_AGENT_NAME=sentinel-dashboard-service
      - SW_AGENT_COLLECTOR_BACKEND_SERVICE=host.docker.internal:11800
      - JAVA_OPTS=-Xms256m -Xmx512m
    networks:
      - app-network
networks:
  app-network:
    driver: bridge