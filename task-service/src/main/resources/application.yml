server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: '*'

dubbo:
  application:
    name: task-service
    qos-enable: false
  registry:
    address: nacos://127.0.0.1:8848
  protocol:
    name: dubbo
    port: 20885