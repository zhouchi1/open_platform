server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: '*'

dubbo:
  application:
    name: task-service
    qos-enable: false
  registry:
    address: nacos-headless.default.svc.cluster.local:8848
    register-mode: instance
  protocol:
    name: dubbo
    port: 20890