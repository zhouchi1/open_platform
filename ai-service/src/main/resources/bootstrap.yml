spring:
  application:
    name: ai-service
  cloud:
    nacos:
      config:
        server-addr: 10.0.0.253:8848,10.0.0.253:8850,10.0.0.253:8852
        # 扩展配置（共享配置）
        extension-configs:
          - data-id: database-dev-config.yaml
            group: DEV_GROUP
            refresh: true
          - data-id: ai-dev-config.yaml
            group: DEV_GROUP
            refresh: true
          - data-id: redis-dev-config.yaml
            group: DEV_GROUP
            refresh: true
      discovery:
        enabled: true
        server-addr: 10.0.0.253:8848,10.0.0.253:8850,10.0.0.253:8852
        namespace: public
  main:
    allow-bean-definition-overriding: true
  config:
    import:
      - optional:nacos:ai-service
  ai:
    dashscope:
      api-key: sk-686bfb9a0ad04b9abcd521325b10ef4c
      chat:
        options:
          model: qwen-plus